---
description: Build or refresh the mutual close plan
argument-hint: [deal name]
---

Invoke the `lock-the-close-plan` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/lock-the-close-plan/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
