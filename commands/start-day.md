---
description: Start your day — today's meetings + 3 deals to move
argument-hint: [rep name, optional]
---

Invoke the `start-the-day` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/start-the-day/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
