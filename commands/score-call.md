---
description: Score a specific sales call + coaching points
argument-hint: "[call name or 'my last [type] call']"
---

Invoke the `score-this-call` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/score-this-call/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
