---
description: Build a CSM handover package for a closed deal
argument-hint: [deal name]
---

Invoke the `hand-off-to-csm` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/hand-off-to-csm/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
