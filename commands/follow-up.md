---
description: Draft the follow-up email to send — in your voice, grounded in real call commitments
argument-hint: [deal / account / call + who it's to]
---

Invoke the `write-the-follow-up` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/write-the-follow-up/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
