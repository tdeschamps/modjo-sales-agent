---
description: Find CRM hygiene issues distorting your forecast
argument-hint: [rep name, optional]
---

Invoke the `audit-the-forecast` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/audit-the-forecast/SKILL.md` for the full instructions, then run it with the user's input below.

User input: $ARGUMENTS
